---
name: assign-agent-roles
description: 複数のエージェントへmanager、advisor、worker、reviewer、researcherの役割を割り当て、それぞれの成果物、権限、越えてはいけない線を決める。「エージェントに役割を振って」「レビュー役を立てて並行で進めたい」と言われたときに使う。agent fleetの起動やpaneの操作はしない。
---

# assign-agent-roles

役割は作業の手順ではなく、産出する成果物の型で分ける。この skill が持つのは、役割を立てるときにエージェントが外しやすい三つの判断である。

## 役割は、その成果物が仕事に要るときだけ立て、兼任で埋めない

manager は目的、完了条件、停止条件と、最終の判断を持つ。advisor は選択肢とトレードオフを返し、合否は決めない。worker は成果物と検証の結果を返し、自分の成果物を評価済みにしない。reviewer は作業の経緯から独立した、再現できる反証を返し、直さない。researcher は出典と時点のある事実を返し、推奨はしない。

全部の役割を毎回立てる必要はない。立てるのは、その役割が産む成果物が仕事に要るときだけである。要る役割の担い手がいないときは、別の役割に暗黙に兼ねさせない。advisor が自分の助言した成果物を review すること、worker が自分の成果物を review することは越境である。担い手がいなければ、足りない役割と、それで未検証になる範囲を報告する。どの役割が要るかが完了条件から一つに決まらないときは、最も筋の良い割り当てを仮説として採り、採らなかった割り当てと、省いたときに未検証になる範囲を添えて確かめる。

エージェントが一体だけの仕事には、役割の割り当てを使わない。そのエージェントは成果物を作る役だけを担い、受け入れと統合は利用者が決める。

## base へ統合できるのは manager だけで、worker は完了報告で止まる

共有の統合先（base branch）へ成果物を統合できるのは、受け入れを決める manager だけである。受け入れる者と統合する者が分かれると、受け入れていない成果物が統合先に入るからである。一者が受け入れと統合の順を持てば、どの成果物をどの順に入れ、残りの作業者にいつ追従させるかも一つに決まる。worker は完了報告で止まり、自分の成果物を統合しない。worker が片付けてよいのも、自分に割り当てられた作業場所と作業 branch だけである。

統合できる役割であることと、統合してよいことは別の問いである。統合してよいかは利用者が決める。利用者が統合を自分に留めているなら、manager は受け入れと統合の順を提案して止まる。利用者の承認をどう扱い、どう渡すかは agent-work-policy に従う。

## reviewer の反証は、受け入れの前に置く

reviewer の指摘の採否は manager が決め、見ていない範囲を「問題なし」に変えない。受け入れは review の後に来る。だから review を、review される成果物の受け入れを前提にした順番に組まない。受け入れた後の review は、受け入れを止められないからである。

## 役割の Catalog は、別の実行の仕組みへ渡すときだけ使う

役割と関係は `../../roles/catalog.yml` に機械が読む形で置いてある。agent fleet のような実行の仕組みへ渡すときだけ、`python3 ../../scripts/export_catalog.py --target "$HOME/.config/agent-roles/catalogs/builtin@2.json"` で検査済みの JSON を書き出して渡す。失敗したら理由を変えずに返す。既に違う内容があると止まるので、差分を確かめて版の扱いを決めてから `--replace` を付ける。Catalog へ書くのは役割の定義と関係だけで、エージェントの実体、model、task、pane のような実行の関心は書き足さない。
